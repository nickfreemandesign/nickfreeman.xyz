import React from 'react';
import styled from 'styled-components';

const MapContainer = styled.div`
    grid-area: map;
`;

const Map = (props) => {
    return (
        <MapContainer>
            map
        </MapContainer>
    )
}

module.exports = Map